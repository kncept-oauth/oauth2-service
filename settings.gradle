rootProject.name='Kncept Oauth2 Service'

// include any subfolders with a build.gradle file in them
for(file in rootDir.listFiles()) {
    if (file.isDirectory() && new File(file, "build.gradle").exists())
        include file.name
}